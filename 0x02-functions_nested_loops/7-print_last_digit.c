#include "main.h"

/**
 * print_last_digit - prints anumber last digit
 * @n: number is an integer
 * Return: last digit
 */
int print_last_digit(int n)
{
	int l;

	l = n % 10;
	if (1 < 0)
	{
		_putchar(-1 + 48);
	return (-1);
	}
	else
	{
	_putchar(1 + 48);
	return (1);
	}
}
